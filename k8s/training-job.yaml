apiVersion: batch/v1
kind: Job
metadata:
  name: soyr-redhat-erebus-v2-train
  namespace: machine-learning
  labels:
    app: soyr-redhat-erebus-v2
    owner: soyr-redhat
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: soyr-redhat-erebus-v2
        owner: soyr-redhat
    spec:
      restartPolicy: OnFailure
      # Pin to A100 nodes
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nvidia.com/gpu.product
                    operator: In
                    values:
                      - NVIDIA-A100-SXM4-80GB
      containers:
        - name: trainer
          image: image-registry.openshift-image-registry.svc:5000/machine-learning/erebus-v2:latest
          command: ["torchrun"]
          args:
            - "--nproc_per_node=4"
            - "--standalone"
            - "scripts/train.py"
            - "--output_dir=/data/checkpoints"
            - "--tensorboard_dir=/data/logs"
            - "--hf_repo=soyrsoyr/erebus-v2-500m-base"
          env:
            - name: HF_HOME
              value: /data/hf_cache
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: soyr-redhat-erebus-v2-secrets
                  key: hf-token
                  optional: true
            - name: TOKENIZERS_PARALLELISM
              value: "false"
            - name: NCCL_DEBUG
              value: "WARN"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: TRITON_CACHE_DIR
              value: /tmp/.triton
            - name: PYTORCH_CUDA_ALLOC_CONF
              value: "expandable_segments:True"
          resources:
            requests:
              nvidia.com/gpu: "4"
              cpu: "32"
              memory: 128Gi
            limits:
              nvidia.com/gpu: "4"
              cpu: "48"
              memory: 192Gi
          volumeMounts:
            - name: data
              mountPath: /data
            - name: shm
              mountPath: /dev/shm
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: soyr-redhat-erebus-v2-data
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 32Gi
